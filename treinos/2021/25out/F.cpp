#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using vi = vector<int>;
using pii = pair<int,int>;

const int MxN = 5123;
const int MOD = 1e9 + 7;


int rates[MxH]

int main(int argc, char const *argv[]) {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int Y, N, S;
	cin >> Y >> N >> S;

	int H, M;
	cin >> H >> M;

	for (int i = 0; i < M; ++i)
	{
		cin >> h >> b >> e >> c;
	}

	return 0;
}