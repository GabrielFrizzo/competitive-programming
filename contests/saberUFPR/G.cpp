#include <bits/stdc++.h>

using ll = long long;
using vi = vector<int>;
using pii = pair<int, int>;

void solve() {
	
}

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int N;
	cin >> N;

	while (N--) {
		solve();
	}

	return 0;
}